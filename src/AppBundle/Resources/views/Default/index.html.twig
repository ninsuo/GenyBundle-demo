{% extends 'FuzQuickStartBundle::layout.html.twig' %}

{% block body %}

    {% for data in views %}

        <pre>{{ data.source }}</pre>

        <br/>

        {% if data.form|default(false) %}
            {{ form(data.form) }}
        {% endif %}

        <br/><br/>

    {% endfor %}

{% endblock %}
