{% extends 'FuzQuickStartBundle::layout.html.twig' %}

{% block body %}

    <div class="row">

        <div class="col-md-6">
            <div class="text-center h2">Builder</div>
            {{ render(controller('GenyBundle:Builder:render', {id: 42})) }}
        </div>

        <div class="col-md-6">
            <div class="text-center h2">Preview</div>
            {{ render(controller('GenyBundle:Renderer:render', {id: 42})) }}
        </div>

    </div>

{% endblock %}
